<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <GenerateTargetFrameworkAttribute>false</GenerateTargetFrameworkAttribute>
  </PropertyGroup>

  <ItemGroup>
    <!-- Exclude only Program.cs — the other McpServer files are compiled into this project -->
    <Compile Remove="CustomResoManager.McpServer\Program.cs" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="ModelContextProtocol.AspNetCore" Version="*-*" />
    <!-- YAML parsing for config.yaml (server bind address). -->
    <PackageReference Include="YamlDotNet" Version="16.*" />
  </ItemGroup>

  <ItemGroup>
    <!-- Ship config.yaml next to the exe so ServerConfig.Load() finds it. -->
    <None Update="config.yaml" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
